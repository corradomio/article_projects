per ogni DAG (ordine v = |V|)
    100 modelli
        per ogni modello,
            a) dataset di 1000? record
            b) dataset di 3*2^v record?

DAG:
    0-indegree:
        variabili indipendenti.
        NON serve aggiungere il rumore
    k-indegree:
        scelta di una funzione booleana con k-parametri random (una su 2^2^k)
        rumore bernulliano B(p) con p scelto random in range [0-0.3]


NB: si potrebbe rappresentare TUTTO in modo compatto

Si e' ineressati SOLO alla struttura, NON alla funzione vera e propria
Analisi sensibilita': dataset
