Installazione
-------------

    usare Python 3.10, per compatibilita' con alcune librerie usate

    "requirements_causal.txt" contiene la lista delle dipendenze.
    
    La corrente configurazione usa Pytorch per CPU, comunque sono gia' 
    presenti (commentate) le direttive per installare Pytorch con supporto alla GPU